title: Crypto Tournament
server:
  host: localhost
  port: 5051
project:
  folder: (env) => env.path.resolve(env.process.cwd(), '../Source')
webscript:
  folder: (env) => env.path.join(env.process.cwd(), "../../../../../Shared/WebScript")
websiteHost:
  folder: (env) => env.path.resolve(env.process.cwd(), '../../../../../Projects/WebsiteHost')
static:
  folder: (env) => env.path.join(env.process.cwd(), "../")
handler: | #js
  (ctx) => {
    return async (req, res, data) => {
    };
  }
