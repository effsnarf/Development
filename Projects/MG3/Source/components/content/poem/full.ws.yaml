dom:
  .box:
    .line:
      v-for: (line, index) in lines
      ':class': getLineCssClass(index)
      v-text: line
props:
  lines: null
methods:
  getLineCssClass: |
    function(index) {
      if (!index) return null;
      return `ml-l${index}`;
    }
name: content-poem-full
style:
  .box:
    margin: auto
    width: fit-content
    padding: 1rem
    '-webkit-mask-image': >-
      -webkit-gradient(linear, left top, left bottom, from(rgba(0,0,0,0.3)),
      to(rgba(0,0,0,1)))
  .line:
    line-height: 1em
    font-size: 200%
    font-family: Old Standard TT
template: >-
  <div class="comp-content-poem-full box"><div class="line" v-for="(line, index)
  in lines" :class="getLineCssClass(index)" v-text="line"></div></div>
