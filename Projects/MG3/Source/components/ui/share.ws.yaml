dom:
  .sharethis-inline-share-buttons:
    ':data-url': url
    ':data-image': imageUrl
props:
  instance: null
mounted: | #js
  function() {
  }
computed:
  url: | #js

    function() {
      return this.$root.url.instance(this.instance, true);
    }
  imageUrl: | #js

    function() {
      return this.$root.url.instanceImage(this.instance);
    }
name: ui-share
template: >-
  <div class="comp-ui-share sharethis-inline-share-buttons" :data-url="url"
  :data-image="imageUrl" path="-2124674043."></div>
