dom:
  component:
    v-if: builder
    ':is': $root.getBuilderComponentName(builder)
    ':editable': true
    ':builder': builder
    ':key': builder._id
    @cancel: $emit('cancel')
props:
  builder: null
name: e-builder
template: >-
  <component class="comp-e-builder" v-if="builder"
  :is="$root.getBuilderComponentName(builder)" :editable="true"
  :builder="builder" :key="builder._id" @cancel="$emit('cancel')"
  path="926534418."></component>
