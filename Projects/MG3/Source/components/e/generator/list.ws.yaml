dom:
  transition.group:
    name: list
    tag: ul
    li:
      v-for: generator in generators
      ":key": generator._id
      e.generator.wide2:
        ":generator": generator

props:
  generators: null

data:

watch:

style:
  li:
    margin-top: 0.5em
