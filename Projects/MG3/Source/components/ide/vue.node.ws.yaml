dom:
  div:
    ui.title:
      icon: 📦
      ':text': getText()
      ':number': item?.$children.length
props:
  value: null
methods:
  getText: | #js

    function() {
      return (this.item?.$options?._componentTag || this.item?._vnode.tag);
    }
computed:
  item: | #js

    function() {
      return (!this.value) ? null : this.value();
    }
name: ide-vue-node
template: >-
  <div class="comp-ide-vue-node"><ui-title icon="📦" :text="getText()"
  :number="item?.$children.length"></ui-title></div>
