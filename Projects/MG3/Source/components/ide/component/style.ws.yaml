dom:
  style:
    ':class': component.name
    v-html: htmlDecode(style)
props:
  component: null
methods:
  htmlDecode: | #js

    function(input) {
      var doc = new DOMParser().parseFromString(input, "text/html");
      return doc.documentElement.textContent;
    }
computed:
  style: | #js

    function() {
      if (!this.component?.source?.style) return "";

      const template = this.$root.templates.style;
      const css = Handlebars.compile(template)(this.component);

      return css;
    }
name: ide-component-style
template: >-
  <style class="comp-ide-component-style" :class="component.name"
  v-html="htmlDecode(style)"></style>
