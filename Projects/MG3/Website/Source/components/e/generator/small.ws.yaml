dom:
  ui.link:
    class: grayish
    :url: "$root.url.generator(item)"
    .badge:
      ui.image:
        :imageID: item.imageID
props:
  item:

style:
  .title:
    font-size: 120%
  .badge:
    display: flex
    margin-top: 1rem
  .comp-ui-image:
    width: 20% !important
    margin-right: 1rem
