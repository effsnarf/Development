dom:
  ui.link:
    :url: "$root.url.generator(item.urlName)"
    layout.horizontal.1:
      ui.image:
        :imageID: item.imageID
      layout.vertical.1:
        text: item.displayName
        number: item.instancesCount
props:
  item:
