dom:
  div:
    v-if: item
    .lines:
      .line.mono:
        v-for: line in item
        v-text: line

props:
  item:

methods:

style:
  .comp-content-poem-stanza:
    background: "#ffffff30"
    width: fit-content
  .lines:
    padding: 1rem
