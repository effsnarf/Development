dom:
  div:
    page.home:
      v-if: $root.params.page == 'home'
    page.generator:
      v-if: $root.params.page == 'generator'
    page.instance:
      v-if: $root.params.page == 'instance'
name: app

style:
  .comp-app:
    margin: auto
