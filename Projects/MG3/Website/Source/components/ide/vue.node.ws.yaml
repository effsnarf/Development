dom:
  ui.title:
    icon: ðŸ“¦
    ':text': getText()
    ':number': item?.$children.length
props:
  value: null
methods:
  getText: |
    function() {
      return (this.item?.$options?._componentTag || this.item?._vnode.tag);
    }
computed:
  item: |
    function() {
      return (!this.value) ? null : this.value();
    }
name: ide-vue-node
template: >-
  <ui-title class="comp-ide-vue-node" icon="ðŸ“¦" :text="getText()"
  :number="item?.$children.length"></ui-title>
