dom:
  flow.module:
    icon: ⚡
methods:
  flow_ui_toolbar_select_box_mark: | #js
    function(rect) {
      this.$femit("create.box", rect);
    }
  onAllEvents: | #js
    function(compName, emitName, args) {
      return;
      if (compName === "flow.ui.toolbar") {
        this.$femit(emitName, ...args);
      }
    }
_:
  examples:
    count: 0
name: flow-ui-events
template: >-
  <flow-module class="comp-flow-ui-events" icon="⚡"
  path="-1573528333."></flow-module>
