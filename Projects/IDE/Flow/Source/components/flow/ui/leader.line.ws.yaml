dom:
  div: 
    div: 
      v-for: toVue in getToVues(toNode)
      ui.leader.line:
        ':from-vue': fromVue
        ':to-vue': toVue
        ':end-plug': endPlug
props:
  fromVue: null
  toNode: null
methods:
  getToVues: | #js
    function(node) {
      let nodeVues = this.$root.getNodeVues(node);
      nodeVues = nodeVues.filter(vue => (vue.$options.name == "flow-app-comp-inst"));
      return nodeVues.map(v => () => v);
    }
computed: null
_:
  examples:
    count: 0
name: flow-ui-leader-line
template: >-
  <div class="comp-flow-ui-leader-line" path="-523715041."><div v-for="toVue in
  getToVues(toNode)" path="-523715041.0"><ui-leader-line :from-vue="fromVue"
  :to-vue="toVue" :end-plug="endPlug"
  path="-523715041.0.0"></ui-leader-line></div></div>
