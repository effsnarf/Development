dom:
  flow.node.base:
    ':node': node
    flow.app.node:
      v-for: (childNode, index) in childNodes
      ':key': childNode.id
      ':node': childNode
props:
  node: null
computed:
  childNodes: | #js
    function() {
      return this.$gdb.getNodes('child.of', this.node)
        .filter(node => !node.type.startsWith('flow.data'));
    }
style:
  .comp-flow-layout-view: null
_:
  examples:
    count: 0
name: flow-layout-view
template: >-
  <flow-node-base class="comp-flow-layout-view" :node="node"
  path="1843545641."><flow-app-node v-for="(childNode, index) in childNodes"
  :key="childNode.id" :node="childNode"
  path="1843545641.0"></flow-app-node></flow-node-base>
