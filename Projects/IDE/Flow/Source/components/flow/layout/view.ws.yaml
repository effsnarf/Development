dom:
  div:
    flow.app.node:
      v-for: (childNode, index) in childNodes
      ':key': childNode.id
      ':node': childNode
props:
  node: null
computed:
  childNodes: | #js
    function() {
      return this.$root.gdb.getNodes('child.of', this.node);
    }
style:
  .comp-flow-layout-view:
    min-width: 30em
_:
  examples:
    count: 0
name: flow-layout-view
template: >-
  <div class="comp-flow-layout-view" path="1843545641."><flow-app-node
  v-for="(childNode, index) in childNodes" :key="childNode.id" :node="childNode"
  path="1843545641.0"></flow-app-node></div>
