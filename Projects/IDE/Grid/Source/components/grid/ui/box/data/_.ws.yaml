dom:
  grid.module:
    icon: 🧊
    ':show-title': 'false'
    ui.value:
      ':value': dataValue
props:
  box: null
data:
  dataValue: null
methods:
  init: | #js
    function(box) {
      this.boxValue = box.value;
    }
  grid_client_runtime_socket_data_change: | #js
    function(boxID, sDirection, sName, sValue) {
      if (boxID != this.box._id) return;
      if (sDirection != "input") return;
      if (sName != "data") return;
      this.dataValue = sValue;
    }
computed:
  icon: | #js
    function() {
      const icons = [];
      const value = this.boxValue;

      icons.add("🧊");
      
      if (typeof value === "string") {
        icons.add("🆎");
        const isUrl = value.startsWith("http");
        if (isUrl) icons.add("🔗");
      }

      return icons
        .filter((icon) => icon)
        .join("");
    }
watch:
  box:
    handler: | #js
      async function(box) {
        this.init(box);
      }
    immediate: true
  boxValue:
    handler: | #js
      async function(value) {
      }
  icon:
    handler: | #js
      async function(icon) {
        this.$emit("update:icon", icon);
      }
    immediate: true
_:
  examples:
    count: 0
name: grid-ui-box-data
template: >-
  <grid-module class="comp-grid-ui-box-data" icon="🧊" :show-title="false"
  path="1558979935."><ui-value :value="dataValue"
  path="1558979935.0"></ui-value></grid-module>
