dom:
  div:
    .page-header:
      app.generator.small:
    .page-content:
      ui.list:
        class: instance-list
        :items: instances
        app.instance.large:

props:
  urlName:
  pageIndex:

data:

mounted: | #js
  async function() {
  }

methods:
  init: | #js
    async function() {
    }
  getMockupInstances: | #js
    function() {
      return Array(10).fill(null);
    }

computed:
  instances: | #js
    function() {
      return this.getMockupInstances();
    }

style:
  .instance-list:
    gap: 4vh
