dom:
  div:
    .page-header:
      app.generator.small:
    ui.list:
      class: instance-list
      app.instance.large:

props:
  urlName:
  pageIndex:

data:

mounted: | #js
  async function() {
  }

methods:
  init: | #js
    async function() {
    }

computed:

style:
  .instance-list:
    margin: 3vh 0
    gap: 4vh
