dom:
  div:
    app.data:
      :urlName: urlName
      v-model:pageData: pd
    .page-header:
      app.generator.small:
    .page-content-container:
      .page-content:
        app.instance.create:
          :generator: pd?.generator

props:
  urlName:

data:
  pd:

mounted: | #js
  async function() {
  }

methods:
  init: | #js
    async function() {
    }

computed:

style:
