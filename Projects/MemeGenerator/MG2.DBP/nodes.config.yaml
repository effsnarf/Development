title:
server:
  host: localhost
  port:
  cors:
    dev: [localhost]
    prod: [memegenerator.net, localhost]
  # on dev, we're relaying requests to the production dbp/db
  proxy:
database:
  connectionString: mongodb://localhost:27017
  query:
    max:
      docs: 50
analytics:
  min:
    elapsed: 50
  database:
    path: F:\Database\MG-Analytics
