dom:
  div:
    class: example-layout
    :style: "{ gap }"
    div:
      v-for: i in boxes
      class: box
      :style: "{ 'aspect-ratio': (boxAspect) }"

props:
  boxes: 3
  boxAspect: 1
  gap: 0.1rem

style:
  .example-layout:
    display: flex

  .box:
    background: gray
    width: 3rem
