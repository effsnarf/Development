dom:
  div:
    ui.dom.grid:
      :grid: grid
      :show-example: "true"
      :show-grid-name: "true"

data:
  grid:
    name: list-x
    template:
      columns: "repeat(3, 1fr)"
    example:
      boxes: 3
      aspect:
        ratio: 3 / 1
