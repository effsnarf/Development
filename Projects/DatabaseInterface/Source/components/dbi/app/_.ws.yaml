dom:
  div:
    div:
      dbi.app.logo:
    .flex:
      ui.drawer:
        dbi.db.select:
      ui.drawer:
        dbi.entity.list:
          v-model: selectedEntity
      div:
        h2:
          v-if: selectedEntity
          ui.title:
            icon: 📁
            :text: selectedEntity.name
            :number: selectedEntity.count
        dbi.entity.items:
          v-if: selectedEntity
          :entity: selectedEntity

data:
  selectedEntity: null

mounted: | #js
  function() {
  }

methods: null

style:
