dom:
  div:
    div:
      dbi.app.logo:
    .flex.gap-1:
      dbi.db.select:
        v-model: selectedDbp
      transition:
        name: slide-hor
        dbi.db:
          v-if: selectedDbp
          :dbp: selectedDbp

data:
  selectedDbp: null

mounted: | #js
  function() {
  }

methods: null

style:
