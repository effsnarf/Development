dom:
  div:
    v-if: task
    div:
      div:
        .clickable:
          @click: () => is.open = !is.open
          h2:
            ui.title:
              icon: ❔
              :text: task.title
        div:
          transition:
            name: slide
            div:
              v-if: is.open
              div:
                v-if: "false"
                ui.drawer:
                  direction: "horizontal"
                  .my-l1:
                    v-for: p in task.analysis
                    .flex:
                      div:
                        v-text: "'💡'"
                      div:
                        v-text: p.text
              .solutions:
                .flex.flex-column:
                  dbi.ai.task.solution:
                    v-for: solution in task.solutions
                    :dbp: dbp
                    :task: task
                    :solution: solution
props:
  dbp: null
  task: null

data:
  is:
    open: true

methods:

style:
  .solution .comp-ui-title div:not(.wrap):
    white-space: wrap
    text-wrap: normal