dom:
  studio.doc.node:
    :node: compNode.children[0]
    :sui: sui
    :data-item: dataItem

props:
  node:
  sui:
  dataItem:

methods:

computed:
  compNode: | #js
    function() {
      return this.$root.getCompNode(this.node);
    }
