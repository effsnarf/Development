dom:
  studio.doc.node:
    :node: compNode.children[0]
    :sui: sui
    :user-app-state: userAppState
    :data-item: dataItem

props:
  userAppState:
  node:
  sui:
  dataItem:

methods:

computed:
  compNode: | #js
    function() {
      return this.$root.getCompNode(this.node);
    }
