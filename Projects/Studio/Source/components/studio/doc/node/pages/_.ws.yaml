dom:
  div:
    v-if: visiblePageNode
    transition:
      name: slide
      :key: visiblePageNode.id
      studio.doc.node:
        :node: visiblePageNode
        :sui: sui
        :key: visiblePageNode.id

props:
  node:
  sui:

methods:

computed:
  visiblePageNode: | #js
    function() {
      return this.node.children.find(c => (c.id == this.sui.page.selected?.id));
    }
