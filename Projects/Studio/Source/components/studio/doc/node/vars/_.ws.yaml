dom:
  div:
    div:
      v-for: nodeVar in node.vars
      h3:
        v-if: "false"
        v-text: "(_uid + '_' + node.id + '_' + nodeVar.type + '_' + nodeVar.name)"
      studio.doc.node.vars.var.item:
        :node: node
        :node-var: nodeVar
        :data-item: dataItem
        @update: (value) => $emit('update', nodeVar.name, value)
        :key: "(_uid + '_' + node.id + '_' + nodeVar.name)"

props:
  node:
  nodeVar:
  dataItem:
  userAppState:

data:

mounted: | #js
  function() {
  }

methods:
