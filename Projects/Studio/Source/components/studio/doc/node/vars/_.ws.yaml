dom:
  div:
    component:
      v-for: var1 in node.vars
      :is: "('studio.doc.node.vars.' + var1.type).kebabize()"
      :var1: var1
      :node-var-path: nodeVarPath
      @update: (key, value) => $emit('update', key, value)

props:
  node:
  nodeVarPath:
