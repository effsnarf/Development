dom:
  div:
    :is: "('h'+(node.size||4))"
    v-text: "(text || '❔')"

props:
  node:
  dataItem:
  userAppState:

computed:
  text: | #js
    function() {
      return this.$root.getBoundData(this.userAppState, this.node, this.dataItem, this.node.text);
    }
