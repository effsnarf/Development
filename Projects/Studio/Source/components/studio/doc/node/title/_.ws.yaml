dom:
  div:
    :is: "('h'+node.size)"
    v-text: text

props:
  node:
  dataItem:

computed:
  text: | #js
    function() {
      return this.dataItem?.[this.node.data?.field] ||
        this.node.text ||
        '❔';
    }
