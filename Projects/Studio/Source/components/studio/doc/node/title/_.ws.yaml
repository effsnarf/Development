dom:
  div:
    :is: "('h'+(node.size||4))"
    v-text: "(text || '❔')"

props:
  node:
  dataItem:

computed:
  text: | #js
    function() {
      return this.$root.getBoundData(this.node, this.dataItem, this.node.text);
    }
