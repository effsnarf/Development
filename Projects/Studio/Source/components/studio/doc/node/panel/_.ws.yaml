dom:
  .flex:
    :style: nodeStyle
    slot:
      name: child-nodes

props:
  node:
  sui:

methods:
  getNodeStyle: | #js
    function(node) {
      const s = {};
      return s;
    }

computed:
  nodeStyle: | #js
    function() {
      return this.getNodeStyle(this.node);
    }
