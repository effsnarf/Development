dom:
  div:
    # :is: "('h'+(node.size||4))"
    :is: "'h3'"
    div:
      v-text: "(text || node.text || '[text❔]')"

props:
  node:
  sui:
  dataItem:
  userAppState:

data:
  text:

mounted: | #js
  function() {
    this.text = this.$parent.$parent.$parent.nodeVarData.text;
  }
