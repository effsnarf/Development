dom:
  div:
    v-if: sui.node.selecteds[0]
    studio.dom.node.browser.box:
      :user-app: sui.node.selecteds[0]
      :sui: sui
      :box-index: 0
      :window-node: sui.page.selected

props:
  userApp:
  sui:
