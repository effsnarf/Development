dom:
  ui.dnd.dropzone:
    :accepts: "['new.page']"
    template:
      v-slot: tooltip
      div:
        v-text: "'create a new page'"
    studio.doc.page.selector:
      :user-app: userApp
      :user-app-state: userAppState
      :sui: sui
    studio.dom.node.browser.box:
      :user-app: userApp
      :sui: sui
      :box-index: 0
      :window-node: sui.node.selecteds?.[0]

props:
  userApp:
  userAppState:
  sui:
