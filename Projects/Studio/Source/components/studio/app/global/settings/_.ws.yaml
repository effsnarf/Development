dom:
  div:
    transition:
      name: slide
      component:
        :is: "'studio-app-global-settings-' + (sui.tool.category.name??'data')"
        :user-app: userApp
        :user-app-state: userAppState
        :sui: sui
        :key: (sui.tool.category.name??'data')

props:
  userApp:
  userAppState:
  sui:

data:
