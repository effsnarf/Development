dom:
  div:
    .flex:
      div:
      .flex:
        span:
          v-text: "'🧊'"
        span:
          v-text: item.name
      div:
        class: preview
        v-if: item.value
        ui.value:
          :value: item.value

props:
  item:

style:
  .preview:
    width: 10em
    max-height: 6em
    overflow: hidden