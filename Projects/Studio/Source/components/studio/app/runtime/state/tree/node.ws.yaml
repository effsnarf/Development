dom:
  div:
    .flex1:
      span:
        v-text: "'🧊'"
      span:
        v-text: item.name
      .dimmed:
        v-if: item.value?.length
        v-text: "'[' + item.value?.length + ']'"
    div:
      v-if: item.value
      .preview:
        ui.value:
          :value: item.value
          :show-visible-fields-filter: "false"

props:
  item:

style:
  .flex1:
    display: flex
    gap: 0.5em
  .preview:
    max-height: 4.5em
    margin-left: 1em
    overflow: auto
    transition: 1s
  .preview:hover:
    max-height: 20em
    