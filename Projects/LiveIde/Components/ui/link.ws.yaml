dom:
  a:
    class: clickable2
    ':target': target
    ':href': '(!enabled ? null : url)'
    @click: onClick
    div:
      v-if: text
      v-text: text
    slot: null
props:
  text: null
  url: null
  target: null
  enabled: true
methods:
  onClick: | #js
    function(e) {
      if (!this.url) return;
      if (this.target) return;
      e.preventDefault();
      e.stopPropagation();
      this.$root.navigateTo(this.url);
      this.$emit("navigate", this.url);
    }
style: null
name: ui-link
template: >-
  <a class="comp-ui-link clickable2" :target="target" :href="(!enabled ? null :
  url)" @click="onClick" path="-484387052."><div v-if="text" v-text="text"
  path="-484387052.0"></div><slot></slot></a>
