dom:
  input:
    type: color
    ':value': value
    @input: $emit('input', $event.target.value)
props:
  value: null
desc:
  value: Use v-model to bind to a color value.
examples:
  - dom:
      div:
        ui.color:
          v-for: (color, i) in colors
          v-model: colors[i]
    data:
      colors:
        - '#ff0000'
        - '#00ff00'
        - '#0000ff'
name: ui-color
_:
  examples:
    count: 1
template: >-
  <input class="comp-ui-color" type="color" :value="value"
  @input="$emit('input', $event.target.value)" path="-2139231351."/>
